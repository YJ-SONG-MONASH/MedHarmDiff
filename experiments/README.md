# Experiments

Keep only lightweight run manifests and summaries in git. Store raw data and large results outside the repository or under ignored `data/` / `results/` directories.

Recommended first run:

```text
feature-level leave-one-center-out benchmark
```

Do not start with image-level diffusion until feature-level baselines are reproducible.
